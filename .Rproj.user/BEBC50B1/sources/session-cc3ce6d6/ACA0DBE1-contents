---
title: "open_specy_tutorial"
format: html
author: >
  Win Cowger, Zacharias Steinmetz, Rachel Kozloski, Aleksandra Karapetrova
date: "`r Sys.Date()`"
---

# Open Specy Tutorial

Welcome to the Open Specy Tutorial! You will learn a common workflow for using the Open Specy package and highlights some topics that users are often requesting a tutorial on. This file will walk you through the process of the following:

- Installation
- Running Open Specy app
- Libraries
- Reading Data
- Open Specy objects
- Visualization
- Spectral Preprocessing Parameters
- Library Matching Spectra
- Additional Resources

The Open Specy R package is the backbone of the Shiny app. The choice is yours as to which you start with, we use both on a regular basis. The tutorial will talk through the R functions you can use to programatically analyze spectra. If you are looking for a tutorial about how to use the app see this [tutorial](https://raw.githack.com/wincowgerDEV/OpenSpecy-package/main/docs/articles/app.html).

# Installation

To install the most up to date and stable Open Specy version 1.5.3.

```{r}
install.packages()
```

# Open Specy App

After installing the R package, you just need to read in the package. This will load in all the functions needed for using Open Specy.

```{r}
library()
```

The package has a built in app that you can use in addition to the [web version](https://openanalysis.org/openspecy/). This will install all current versions of the spectral libraries and Open Specy code and will pop up on your screen.

```{r}
run_app()
```

When loading Open Specy for the first time you might get a message that says: "*Library missing or incomplete: derivative, nobaseline, raw, medoid_derivative, medoid_nobaseline, model_derivative, model_nobaseline; use 'get_lib()' to download a current version*".

# Read Data

The following line of code will read in your data. Open Specy currently is able to read in the following file types:

- .spa/.spc/.asp

- .jdx/.dx/ .0

- .xyz/.csv/.tsv/.txt

- .h5

- ENVI files (.dat & .hdr)

- native Open Specy .y(a)ml, .json, or .rds

- .zip files

.zip files can either contain multiple files with individual spectra in them of the non-zip formats or it can contain a .hdr and .dat file that form an ENVI file for a spectral map. If uploading a .csv file, it is ideal to label the column with the wavenumbers ***wavenumber*** and name the column with the intensities ***intensity***.

```{r}
# Provide the file path of your data
testspectra <- read_any()
```

We can also work with test data

```{r}
# Add direct file path to dataset
data()
```

# Open Specy Objects

You will notice now that the R package reads in files into an object with class OpenSpecy. This is a class we created for high throughput spectral analysis which now also preserves spectral metadata. You can even create these from scratch if you’d like.

```{r}
# seq(n1,n2,by = n): Create a vector sequence of numbers from range n1 to n2 by intervals of n 
# runif(n = ): Generates n random numbers between the range of 0 to 1  
# file_name: name of file that will be listed in the metadata

scratch_OpenSpecy <- as_OpenSpecy(
  x = seq(1000,2000, by =5),
  spectra = data.frame(runif(n = 201)),
  metadata = list(file_name = "fake_noise")
)

```

Open Specy objects are lists with three components, ***wavenumber*** is a vector of the wavenumber values for the spectra and corresponds to the rows in ***spectra*** which is a data.table where each column is a set of spectral intensities. ***metadata*** is a data.table which holds additional information about the spectra. Each row in ***metadata*** corresponds to a column in ***spectra***.

```{r}
# Access the wavenumbers
scratch_OpenSpecy$
```

```{r}
# Access the spectra
scratch_OpenSpecy$
```

```{r}
# Access the metadata

```

We can check to ensure that the OpenSpecy object adheres to our standards.

```{r}
# returns TRUE if it passes. 
check_OpenSpecy(scratch_OpenSpecy)

# Checks only the object type to make sure it has OpenSpecy type
is_OpenSpecy(scratch_OpenSpecy)
```

Some generic functions for inspecting the spectra:

```{r}
# raw object
print()
```

```{r}
# summarize contents of object
summary()
```

```{r}
# Show the frst n number of wavenumbers and intensitites

head()
```

Open Specy objects can be saved most accurately as .csv, .rds, .yml, or .json files. .rds will be the most reproducible as it is a native R file format and floating point errors can happen with .csv, .json, or .yml.

```{r}
# Save as yml
write_spec()

# Save as JSON file
write_spec(scratch_OpenSpecy, "test_scratch_OpenSpecy.json", digits = 5)

# Save as csv
write_spec(scratch_OpenSpecy, "test_scratch_OpenSpecy.csv", digits = 5)

```

# Visualizations

In R, we have two ways to visualize your spectra, one is quick and efficient and the other is interactive. Here is an example of quick and efficient plotting.

```{r}
plot()
```

This is an example of an interactive plot.

```{r}
plotly_spec()

```

# Spectral Maps

Spectral maps can also be visualized as overlaid spectra but in addition the spatial information can be plotted as a heatmap. It is important to note that when multiple spectra are uploaded in batch they are prescribed `x` and `y` coordinates, this can be helpful for visualizing summary statistics and navigating vast amounts of data but shouldn’t be confused with data which actually has spatial coordinates.

```{r}
spectral_map 
```

An interactive plot of the heatmap and spectra overlayed can be generated with the [`interactive_plot()`](https://raw.githack.com/wincowgerDEV/OpenSpecy-package/main/docs/reference/interactive_plots.html) function. A user can hover over the heatmap to identify the next row id they are interested in and update the value of select to see that spectrum.

```{r}

```

# Filtering Open Specy Objects

OpenSpecy objects can have any number of spectra in them. To create an OpenSpecy with a subset of the spectra that is in an Open Specy object you can use the `filter_spec` function. Filtering is allowed by index number, name, or using a logical vector. Filtering will update the `spectra` and `metadata` items of the `OpenSpecy` but not the `wavenumber`.

```{r}
# Extract the 150th spectrum by index number. 
filter_spec()

# Extract the 150th spectrum by spectrum name. 
filter_spec()
# Extract the 150th spectrum by logical test. 
filter_spec()

#Test that they are the same. 
identical()

identical())

```

# Spectral Preprocessing Parameters

The goal of processing is to increase the signal to noise ratio (S/N) of the spectra and remove unwanted artifacts without distorting the shape, position, or relative size of the peaks. After loading data, you can process the data using intensity adjustment, baseline subtraction, smoothing, flattening, and range selection. The default settings is an absolute first derivative transformation. It is really powerful for many data issues. It does something similar to smoothing, baseline subtraction, and intensity correction simultaneously and really quickly.

```{r}

```

You can compare the processed and unprocessed data in the overlay plot.

```{r}
# plotly_spec(x=X1, x2=X2)
# Extracting peaks where I would expect it and reducing the baseline

```

Digging deeper into the process_spec function you can look at the official documentation of the function.

```{r}
?process_spec
```

The `process_spec()` function is a monolithic function for all processing procedures which is optimized by default to result in high signal to noise in most cases, same as the app. This function can be modified to your needs.

Let's say we want to do a baseline correction.

```{r}
no_baseline_ts <- process_spec(
  raman_hdpe,
  active = TRUE,
  adj_intens = FALSE,
  # input spectra type: none(absorbance unit), transmittance, reflectance
  adj_intens_args = list(type = "none"),
  conform_spec = TRUE,
  # setting wave number step size
  conform_spec_args = list(range = NULL, res = 5, type = "interp"),
  restrict_range = FALSE,
  # cropping ranges 
  restrict_range_args = list(min = 1000, max = 2000),
  # remove peaks 
  flatten_range = FALSE,
  flatten_range_args = list(min = 1400, max = 1500),
  #baseline subtraction
  subtr_baseline = TRUE,
  subtr_baseline_args = list(type = "polynomial", degree = 8, raw = FALSE, baseline =
                               NULL),
  smooth_intens = FALSE,
  smooth_intens_args = list(polynomial = 3, window = 11, derivative = 1, abs = TRUE),
  make_rel = TRUE,
  make_rel_args = list(na.rm = TRUE)
)

```

You can compare the processed and unprocessed data in the overlay plot.

```{r}

plotly_spec(x = raman_hdpe, x2 = no_baseline_ts)
```

# Libraries

An alternative way to load libraries by using the get_lib() function. Reference libraries are spectra with known identities. The Open Specy library now has over 30,000 spectra in it and is getting so large that we cannot fit it within the R package size limit of 5 MB. We host the reference libraries on [OSF](https://osf.io/x7dpz/) and have a function to pull the libraries down automatically. Running get_lib by itself will download all libraries to your package directory or you can specify which libraries you want and where you want them.

```{r}
get_lib()
```

After download you can load the libraries into your active environment one at a time as `OpenSpecy` objects. You can use any Open Specy object as a library which makes it easy to work with and create libraries because everything we explained earlier applies to them.

```{r}

```

There are seven library types that can be retrieved, they are:

- `derivative`

- `nobaseline`

- `raw`

- `medoid_derivative`

- `medoid_derivative`

- `medoid_nobaseline`

- `model_derivative`

- `model_nobaseline`

# Identification

After your spectra is processed similarly to the library specifications, you can identify the spectra using [`match_spec()`](https://raw.githack.com/wincowgerDEV/OpenSpecy-package/main/docs/reference/match_spec.html). Whichever library you choose, you need to get your spectra into a similar enough format to use for comparison.

```{r}

```
